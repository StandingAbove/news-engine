# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
websockets>=13.1

# --- Data / scientific ---
# Use versions with prebuilt wheels for Python 3.13 on Apple Silicon.
numpy>=2.1,<3
pandas>=2.2.3,<3
scipy>=1.14,<2

# --- Market data ---
yfinance>=0.2.51

# --- HTTP ---
httpx>=0.27
requests>=2.32
tenacity>=9.0

# --- NLP / sentiment ---
vaderSentiment>=3.3.2

# --- Persistence ---
SQLAlchemy>=2.0.36,<3
aiosqlite>=0.20
greenlet>=3.1  # required by SQLAlchemy async on Python 3.13

# --- Scheduling ---
apscheduler>=3.10

# --- Env / config ---
python-dotenv>=1.0.1
pydantic>=2.9,<3
pydantic-settings>=2.6

# --- Dev / test ---
pytest>=8.3
pytest-asyncio>=0.24

# --- Forecasting sub-component (sub-component A from the Feb-6 email) ---
# These are opt-in. The core news engine + text-feed sub-component run
# without them. To enable the forecasting sub-component (ARIMA / MLP /
# LSTM / TimesFM) install `requirements-forecast.txt`.

